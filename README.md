# my-blog-back-app
REST-Service блог для публикации и управления постами, с возможностью
оставлять и редактировать комментарии, ставить лайки и т.д. 
## Описание проекта
Этот проект - бэкенд часть приложения блога. Основная функцмиональность заключается во взаимодействии с постами и и их комментариями через соответсвующие эндпоинты:

1. #### Базовые эндпоинты для постов
 - 🚀 ```GET /api/posts/{id}```      -  получение поста по id.
 - 📝 ```POST /api/posts```        -  создание нового поста на основании JSON в теле запроса.
 - 🔄 ```PUT /api/posts/{id}```    -  обновление поста с id используется тело запроса(JSON)
 - ❌ ```DELETE /api/posts/{id}``` -  удаление поста по id(с комментариями).
2. #### Базовые эндпоинты для комментариев
 - 🚀 ```GET /api/posts/{id}/comments/{id}```      -  получение комментария по id поста и комментария.
 - 📝 ```POST /api/posts/{id}/comments```        -  создание нового комментария для поста с id на основании переданного тела запроса (JSON).
 - 🔄 ```PUT /api/posts/{id}/comments/{id}```    -  обновление комментария с по id поста и комментария, используется тело запроса(JSON)
 - ❌ ```DELETE /api/posts/{id}``` -  удаление комментария для конкретного поста по его id и id комментария.

 3. #### Эндпоинты для работы с картинками:
      Отельно хотелось бы сказать про дополнительный функционал, например для поста вы можете не просто задать текстовые поля , а еще 
 рабоать с изображениями постов, сохранить а также получить её.

   ```PUT /api/posts/id/image```  - обновление картинки
   
   ```GET /api/posts/id/image```  - получение картинки.
  
5. #### Эндпоинт динамического поиска
  Позвоялет искать посты с помощью переменного условия, тоесть поиск возможен как по тегам(#SomeTag), если они переданны, так и по названию поста(postName). Если передеано что то одно, напрмиер, теги отсутсвуют , то поиск пройдет по названию поста. Также в запросе передаётся номер страницы и число элементов, для пагинации. В ответ пользователь получит JSON с перечнем постов и информацией по странице.
  
   ``` GET /api/posts?search=postName #SomeTag&pageNumber=1&pageSize=5```
   
5. #### Увеличне числа лайков для поста:
   ```/api/posts/id/likes```

### Пример применения
 
   Рассмотрим пример, иллюстрирующий применение приложения, в котором был добавлен пост о пользе книг, заполнили всю информацию и выбрали картинку.
 
![alt text](https://github.com/AlexLakers/ParserJsonCsvToXml/blob/master/WinFormsCsvJsonXml/App_Data/pictures/blog1.png?raw=true)

Как мы видим, пост о пользе книг создался, есть комментарий и даже несколько лайков.

А тут вы можете наблюдать как этот пост был найден с помощью удобного поиска по частям названия поста и тегу.

![alt text](https://github.com/AlexLakers/ParserJsonCsvToXml/blob/master/WinFormsCsvJsonXml/App_Data/pictures/blog2.png?raw=true)

 Хотелось бы отметить, что в данном случае это приложение используется совместно с конкретным фронтенд приложением, но можно использовать его
 с любым REST клиентом, главное вызвать нужные ендпоинты пописанные выше.

 ### Основные компоненты

  > - В качестве основного фреймворка был использован Spring Boot v3.5.8.
  > - В качестве БД я использовал  PostgreSQL V 17.0, для тестов Ин-мемори БД H2.
  > - В качестве системы сборки я использовал  Gradle.
  > - Для запуска фронтенда использовал Docker 27.3.1.
  > - Java 21.
  > - В качестве контейнера сервлетов - Tomcat(embedded) 
  > - Для маппинга использовал Mapstruct framework.
  > - Для валидации использовал Hibernate Validator. 

### Сборка
Сборка этого проекта была переведена с ручной на инфраструктуру Docker с использованием Docker compose. 

  - В корневой папке проекта вы найдете файлы для сборки: ```Dockerfile```, ```compose.yaml```
 Для сборки и запуска приложения просто запустите из корня проекта следующую команду.
 
  ```
  docker compose up -d
  ```
    
  - В результате поднимется база данных Postgres и само приложение во внетренней сети Docker.

> [!IMPORTANT]
  Обратите внимание , вам надо создать файл ```.env``` с перемнными, которые используются в ```compose.yaml```

- Вы можете использовать любой HTTP-client или frontend приложение ```my-blog-front-app```, папка лежит в корне проекта.
    Для запуска вам нужно просто перейди в папку с фронт-приложением и поднять Nginx, который будет проксировать запросы.
    
```
docker compose up -d
```

Для запуска тестов 
```
./gradlew test
```



