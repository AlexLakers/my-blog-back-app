services:
  database:
    image: postgres:17.0
    ports:
       - "5442:5432"
    restart: always
    environment:
       - POSTGRES_USER=${DB_USER}
       - POSTGRES_PASSWORD=${DB_PASS}
       - POSTGRES_DB=${DB_NAME}
    volumes:
      - db-data:/var/lib/postgresql/data


  blog-service:
    build:
      context: .
      dockerfile: Dockerfile
    image: blog
    restart: always
    environment:
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
    ports:
      - "8080:8080"
    depends_on:
      - database

volumes:
  db-data: